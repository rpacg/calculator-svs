# SVS Calculator v9.3.20 Update Log

## Change summary / Ringkasan perubahan
- Created new file: `Score Calculator SVS v9.3.20.html`
- Dibuat file baru: `Score Calculator SVS v9.3.20.html`
- Updated title and footer metadata from `v9.3.19` to `v9.3.20`
- Memperbarui metadata judul dan footer dari `v9.3.19` ke `v9.3.20`
- Fixed jungler toggle handling so the jungler-type buttons only remain active when the player role is `Jungler`
- Memperbaiki logika toggle jungler sehingga tombol tipe jungler hanya aktif saat peran player adalah `Jungler`
- Ensured role button update logic resets jungler toggle state when switching away from `Jungler`
- Menjamin logika pembaruan tombol peran mereset status toggle jungler saat berpindah dari `Jungler`
- Added `updateAllTfpHints()` call after `setJunglerType()` to refresh TFP helper text immediately
- Menambahkan pemanggilan `updateAllTfpHints()` setelah `setJunglerType()` agar teks bantuan TFP segera diperbarui

## Reason for change / Alasan perubahan
This update isolates the `v9.3.20` bug fix into a fresh version file while preserving previous stable versions for rollback.
Update ini memisahkan perbaikan bug `v9.3.20` ke file versi baru sambil mempertahankan versi stabil sebelumnya untuk rollback.

## Notes / Catatan
- The fix improves UI behavior and prevents stale toggles when changing player roles.
- Perbaikan ini meningkatkan perilaku UI dan mencegah toggle usang ketika peran player diubah.
- `v9.3.20` is a new versioned release file built from `v9.3.19`.
- `v9.3.20` adalah file rilis versi baru yang dibuat dari `v9.3.19`.
